name: Lab-014.yaml

on:
  push:
    branches:
      - main
      - mkdocs
      - gh-pages
    paths:
      - "Labs/014-composite-actions/**"
  pull_request:
    branches:
      - main
      - mkdocs
      - gh-pages
    paths:
      - "Labs/014-composite-actions/**"

jobs:
  build:
    runs-on: ubuntu-latest
    # Execute the step demo to verify that all is working
    steps:
      # Check out the code
      - uses: actions/checkout@v4

      - name: Test Lab
        working-directory: Labs/014-composite-actions
        run: bash ./_demo.sh
